# PostgreSQL Client Authentication Configuration File
#
# Generated by Ansible
# All handmade changes can be rewrited!
#
# Allow any user on the local system to connect to any database with
# any database user name using Unix-domain sockets (the default for local
# connections).
#
# TYPE  DATABASE        USER            ADDRESS                 METHOD

{% for user in (postgresql_users_default + postgresql_users + postgresql_users_replication) %}
{% for connection in user.connections | default([]) %}
{{ connection.type }} {{ connection.database }} {{ user.user }} {{ connection.address | default('') }} {{ connection.auth }}
{% endfor %}
{% endfor %}
