# Generated by Ansible
# All handmade changes can be rewrited!

setuid {{ postgresql_superuser }}
setgid {{ postgresql_superuser }}

respawn
respawn limit 3 30
normal exit 0 TERM

start on (local-filesystems and net-device-up IFACE!=lo)
stop on runlevel [06]

script
exec /usr/bin/repmgrd --verbose --monitoring-history --config-file={{ postgresql_repmgr_conf }}
end script
