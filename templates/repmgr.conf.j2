# Generated by Ansible
# All handmade changes can be rewrited!

cluster={{ postgresql_repmgr_cluster_name }}
node={{ postgresql_repmgr_node }}
node_name={{ postgresql_repmgr_node_name }}
conninfo={{ postgresql_repmgr_conninfo }}

pg_bindir=/usr/lib/postgresql/{{ postgresql_version }}/bin

use_replication_slots=1

# https://github.com/2ndQuadrant/repmgr/blob/master/FAILOVER.rst
master_response_timeout=60

reconnect_attempts=6
reconnect_interval=10

failover=automatic
promote_command='/usr/bin/repmgr-promote.sh'
follow_command='repmgr standby follow -f {{ postgresql_repmgr_conf }} -W'
